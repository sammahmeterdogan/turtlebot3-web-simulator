# Gradle Configuration
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Java Version
java.version=17

# Application Properties
app.version=1.0.0
app.name=turtlebot3-web-simulator

# Default Database Config (for development)
db.url=jdbc:postgresql://localhost:5432/turtlebot3_db
db.username=samma
db.password=1234

# ROS Configuration
ros.bridge.url=ws://localhost:9090
ros.docker.network=turtlebot-network

# Server Configuration
server.port=8080